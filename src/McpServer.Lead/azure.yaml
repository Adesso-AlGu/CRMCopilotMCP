# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: mcp-lead-server
metadata:
  template: mcp-lead-server@0.0.1-beta
services:
  lead:
    project: ./
    language: dotnet
    host: appservice
