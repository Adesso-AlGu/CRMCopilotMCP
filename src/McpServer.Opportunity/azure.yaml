# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: mcp-opportunity-server
metadata:
  template: mcp-opportunity-server@0.0.1-beta
services:
  opportunity:
    project: ./
    language: dotnet
    host: appservice
