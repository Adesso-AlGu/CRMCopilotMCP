# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: mcp-quote-server
metadata:
  template: mcp-quote-server@0.0.1-beta
services:
  quote:
    project: ./
    language: dotnet
    host: appservice
